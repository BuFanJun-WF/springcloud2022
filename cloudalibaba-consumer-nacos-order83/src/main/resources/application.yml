server:
  port: 83

spring:
  application:
    name: nacos-order-comsumer
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

# 消费者将要访问的服务提供者的微服务
service-url:
  nacos-user-service: http://nacos-payment-provider


