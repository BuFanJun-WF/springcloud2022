server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 # 配置Nacos注册中心地址

# 暴露需要监控的内容
management:
  endpoints:
    web:
      exposure:
        include: '*'